import React from 'react';
import './index.scss';

const Header = () => {
  return (
    <header className="Header">
        <nav className="Nav">
          <a href="/">hi</a>
        </nav>
    </header>
  );
}

export default Header;